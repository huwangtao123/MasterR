" basic configuration file for vim
" created by Tao Wang (huwangtao123@gmail.com) 

" sets how many lines of history  VIM has to remember
set history=700

" Sets line number
set nu

" set backspace
set backspace=2

" Ignore case and be smart when searching 
set ignorecase
set smartcase

" change the backgroup, so the comments will easy to read
set background=dark

"enable syntax highlighting
syntax on


